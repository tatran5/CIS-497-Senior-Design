using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FractalTerrain : MonoBehaviour
{
    // test
    ArrayList triangles;

    // Start is called before the first frame update
    void Start()
    {
        // Test points
        Vector3 p0_1 = new Vector3(-10, 0, 0);
        Vector3 p0_2 = new Vector3(10, 0, 0);
        Vector3 p0_3 = new Vector3(0, 0, 10);
        triangles.add
    }

    // Update is called once per frame
    void Update()
    {
        
    }

}